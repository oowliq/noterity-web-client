import { Theme } from 'interfaces';

const lightTheme: Theme = {
    borders: 'box-shadow: 0 0 16px 0 rgba(36, 37, 38, 0.08);',
    colors: {
        accent: '#ff3838',
        primary: '#f7f7f7',
        secondary: '#fff',
    },
    fontColors: {
        inactive: '#737373',
        disabled: '#000',
        default: '#000',
    },
};

export { lightTheme };
