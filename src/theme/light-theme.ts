import { Theme } from 'interfaces';

const lightTheme: Theme = {
    colors: {
        accent: '#f97f51',
        primary: '#fff',
        secondary: '#fff',
    },
    fontColors: {
        inactive: '#737373',
        disabled: '#000',
        default: '#000',
    },
};

export { lightTheme };
