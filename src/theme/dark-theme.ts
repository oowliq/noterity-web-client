import { Theme } from 'interfaces';

const darkTheme: Theme = {
    colors: {
        accent: '#ff3838',
        primary: '#1a1a1a',
        secondary: '#000',
    },
    fontColors: {
        inactive: '#737373',
        disabled: '#000',
        default: '#fff',
    },
};

export { darkTheme };
