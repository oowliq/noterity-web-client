import React, { FC } from 'react';
import AppSidebar from 'components/AppSidebar';

const Dashboard: FC = () => {
    return (
        <div>
            <AppSidebar />
        </div>
    );
};

export default Dashboard;
