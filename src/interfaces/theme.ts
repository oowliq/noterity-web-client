export interface Theme {
    colors: {
        accent: string;
        primary: string;
        secondary: string;
    };
    fontColors: {
        inactive: string;
        disabled: string;
        default: string;
    };
}
